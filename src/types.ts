export interface Verse{id:string;slug:string;modern:string;historical:string;testament:'Old'|'New';deuterocanonical:boolean;order:number;chapter:number;verse:number;text:string|null;incoming:number;outgoing:number;sourceBooks:number;rank:number;connected?:number;reciprocal?:number;incomingScore?:number;targetBooks?:number;maxIncomingScore?:number;testamentSources?:Record<string,number>;canonSources?:Record<string,number>}
export interface Manifest{schemaVersion:number;generatedAt:string;license:string;fallbackUsed:boolean;datasetTitle?:string;translation?:string;canonicalBooks?:number;verses?:number;versesWithCrossReferences?:number;rawReferences?:number;exactReferences?:number;uniqueEdges?:number;chapterOnly?:number;ambiguous?:number;invalidTargets?:number;excludedBookReferences?:number;selfLinks?:number;exactParsePercentage?:number;coveredBooks?:number;unionRegistryVerses?:number;sourceRows?:number;expandedRelationships?:number;uniqueDirectedEdges?:number;duplicatePairs?:number;invalidRows?:number;missingBsbText?:number;reciprocalPairs?:number;sourceArchiveSha256?:string;sourceMode?:string;sourceGroups?:number;directionalGroups?:number;quotationEvents?:number;invalidPassages?:number}
export interface BookStat{slug:string;order:number;modern:string;historical:string;testament:string;deuterocanonical:boolean;verses:number;incoming:number;outgoing?:number;connected?:number;mean:number;median:number;referencedPercent:number;bestVerse:string|null}
export interface Edge{source:string;target:string;raw?:string[];rows?:string[];score?:number} export interface BookDetail{slug?:string;book?:string;incoming:Record<string,Edge[]>;outgoing:Record<string,Edge[]>}
export interface Quality{exactReferences:number;uniqueEdges:number;exactParsePercentage:number;chapterOnly:number;ambiguous:number;invalidTargets:number;excludedBookReferences:number;selfLinks:number;uniqueAbbreviations:number;unresolvedByBook:{book:string;count:number}[];samples:{source:string;raw:string;reason:string;status:string}[]}
