export interface Verse{id:string;slug:string;modern:string;historical:string;testament:'Old'|'New';deuterocanonical:boolean;order:number;chapter:number;verse:number;text:string;incoming:number;outgoing:number;sourceBooks:number;rank:number;testamentSources?:Record<string,number>;canonSources?:Record<string,number>}
export interface Manifest{schemaVersion:number;generatedAt:string;datasetTitle:string;license:string;translation:string;canonicalBooks:number;verses:number;versesWithCrossReferences:number;rawReferences:number;exactReferences:number;uniqueEdges:number;chapterOnly:number;ambiguous:number;invalidTargets:number;excludedBookReferences:number;selfLinks:number;exactParsePercentage:number;fallbackUsed:boolean}
export interface BookStat{slug:string;order:number;modern:string;historical:string;testament:string;deuterocanonical:boolean;verses:number;incoming:number;mean:number;median:number;referencedPercent:number;bestVerse:string}
export interface Edge{source:string;target:string;raw:string[]} export interface BookDetail{slug:string;incoming:Record<string,Edge[]>;outgoing:Record<string,Edge[]>}
export interface Quality{exactReferences:number;uniqueEdges:number;exactParsePercentage:number;chapterOnly:number;ambiguous:number;invalidTargets:number;excludedBookReferences:number;selfLinks:number;uniqueAbbreviations:number;unresolvedByBook:{book:string;count:number}[];samples:{source:string;raw:string;reason:string;status:string}[]}
