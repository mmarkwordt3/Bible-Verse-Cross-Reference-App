import type {LayerId} from './data';export type VerseRoute={layer:LayerId;book:string;chapter:number;verse:number;legacy:boolean};export function parseVerseRoute(parts:string[]):VerseRoute|null{if(parts[0]!=='verse')return null;if(parts[1]==='openbible'||parts[1]==='douay'){const chapter=Number(parts[3]),verse=Number(parts[4]);return chapter>0&&verse>0?{layer:parts[1],book:parts[2]||'',chapter,verse,legacy:false}:null}const chapter=Number(parts[2]),verse=Number(parts[3]);return chapter>0&&verse>0?{layer:'douay',book:parts[1]||'',chapter,verse,legacy:true}:null}
