import {access,readFile,stat} from 'node:fs/promises';import {resolve} from 'node:path';import {BOOKS} from './config.mjs';import {OPENBIBLE_BOOKS} from './openbible-config.mjs';const mode=process.argv[2],only=process.argv[3];if(!['public','dist'].includes(mode))throw Error('Usage: node scripts/verify-data-files.mjs <public|dist> [openbible|quotations|greek-reuse]');const root=resolve(mode==='public'?'public/data':'dist/data'),douay=['manifest.json','rankings.json','book-stats.json','data-quality.json',...BOOKS.map(x=>`books/${x}.json`)],open=['layers.json','openbible/manifest.json','openbible/rankings.json','openbible/book-stats.json','openbible/data-quality.json',...OPENBIBLE_BOOKS.map(x=>`openbible/books/${x.id}.json`)];let quotationBooks=[],quotationDiscoveryError;try{const manifest=JSON.parse(await readFile(resolve(root,'quotations/manifest.json'),'utf8'));quotationBooks=manifest.sourceBooks||[]}catch(error){quotationDiscoveryError=error}if(!quotationBooks.length)try{const stats=JSON.parse(await readFile(resolve(root,'quotations/book-stats.json'),'utf8'));quotationBooks=stats.map(x=>x.id)}catch(error){quotationDiscoveryError=new AggregateError([quotationDiscoveryError,error].filter(Boolean),'Unable to inspect quotation book inventory')}if(only==='quotations'&&!quotationBooks.length&&quotationDiscoveryError)console.warn(`Quotation inventory unavailable; core-file verification will report the missing source: ${quotationDiscoveryError.message}`);let greekBooks=[];try{const greekManifest=JSON.parse(await readFile(resolve(root,'greek-reuse/manifest.json'),'utf8'));greekBooks=greekManifest.sourceBooks||[]}catch{}const greekCore=['layers.json','greek-reuse/manifest.json','greek-reuse/passage-rankings.json','greek-reuse/verse-rankings.json','greek-reuse/book-stats.json','greek-reuse/data-quality.json','greek-reuse/config.json','greek-reuse/candidate-index.json'],greek=[...greekCore,...greekBooks.map(id=>`greek-reuse/books/${id}.json`)];const quotationCore=['layers.json','quotations/manifest.json','quotations/rankings.json','quotations/book-stats.json','quotations/data-quality.json','quotations/events.json','quotations/verse-rankings.json','quotations/target-rankings.json'],quotations=[...quotationCore,...quotationBooks.map(id=>`quotations/books/${id}.json`)];const required=only==='openbible'?open:only==='quotations'?quotations:only==='greek-reuse'?greek:[...douay,...open,...quotations,...greek],missing=[],empty=[];for(const path of required)try{await access(resolve(root,path));if((await stat(resolve(root,path))).size===0)empty.push(path)}catch{missing.push(path)}if(missing.length||empty.length)throw Error(`Required ${mode} data is incomplete. Missing: ${missing.join(', ')||'none'}; empty: ${empty.join(', ')||'none'}. Run the bootstrap data workflow before deployment.`);console.log(`Verified ${required.length} ${mode} data assets across the requested layer set.`)
