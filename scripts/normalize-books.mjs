export function plainText(value=''){return String(value).replace(/<[^>]*>/g,' ').replace(/&nbsp;/gi,' ').replace(/&amp;/gi,'&').replace(/&quot;/gi,'"').replace(/&#39;|&apos;/gi,"'").replace(/&lt;/gi,'<').replace(/&gt;/gi,'>').replace(/&#(\d+);/g,(_,n)=>String.fromCodePoint(+n)).replace(/\s+/g,' ').trim()}
export function normalizeBook(raw,slug){const found=[];function walk(x,chapter){if(Array.isArray(x)){x.forEach(y=>walk(y,chapter));return}if(!x||typeof x!=='object')return;const ch=Number(x.chapter??x.chapter_number??chapter),v=Number(x.verse??x.verse_number??x.number);const text=x.text??x.verse_text??x.content;if(Number.isInteger(ch)&&Number.isInteger(v)&&typeof text==='string'){const refs=x.cross_refs??x.crossRefs??[];found.push({id:`${slug}.${ch}.${v}`,slug,chapter:ch,verse:v,text:plainText(text),crossRefs:Array.isArray(refs)?refs.map(r=>plainText(typeof r==='string'?r:r.reference??r.text??'' )).filter(Boolean):[]});return}for(const [k,val] of Object.entries(x))walk(val,/^\d+$/.test(k)?+k:ch)}walk(raw);const unique=new Map(found.map(v=>[v.id,v]));return [...unique.values()].sort((a,b)=>a.chapter-b.chapter||a.verse-b.verse)}
